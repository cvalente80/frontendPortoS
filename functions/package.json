{
  "name": "ansiao-seguros-functions",
  "engines": {
    "node": "^20"
  },
  "type": "module",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "lint": "eslint . --ext .ts"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.25",
    "eslint": "^8.57.0",
    "typescript": "^5.9.3"
  }
}
